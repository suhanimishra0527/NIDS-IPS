"""
WSGI entry point for cloud deployment (Render, PythonAnywhere, Heroku).
Usage: gunicorn wsgi:app
"""
# TODO: Import and create the Flask app for WSGI serving

# from server.app import create_app
# app = create_app()
