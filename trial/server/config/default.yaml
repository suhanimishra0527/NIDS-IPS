# Server configuration
# TODO: Implement YAML config loading

port: 5000
host: "0.0.0.0"
debug: false

database:
  url: "sqlite:///nids.db"
  track_modifications: false

security:
  jwt_secret: "CHANGE_ME_IN_PRODUCTION"
  jwt_algorithm: "HS256"
  jwt_expiry_days: 365
  agent_registration_key: "changeme"

heartbeat:
  timeout_seconds: 90

alerts:
  auto_block_threat_score: 80

blocklist:
  expiry_seconds: 86400
